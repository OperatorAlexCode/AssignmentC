package com.example.assignmentc

class Player(var currentTile: Tile) {
    //var item: Item? = null //TODO: implement item "holding"
    //var health: Int = 3 //TODO: decide on health or instant loss when touched by enemy


    //Commented out because movement is now handled by Maze
    /*fun move(direction: TileDirection) {
        val nextTile = currentTile.GetTile(direction)
        if (nextTile != null && !nextTile.IsWall) {
            currentTile = nextTile
        }
    } */
}